spring:
  main:
    banner-mode: off
  ai:
    mcp:
      server:
        name: banco-mock-mcp
        version: 1.0.0
        transport: SSE

server:
  port: ${PORT:8080}

jwt:
  secret: banco-mcp-server-secret-key-2024-seguro-32b
  expiration: 86400000

mcp:
  api-key: ${MCP_API_KEY:mcp-key-local-dev}

logging:
  level:
    root: INFO
    com.banco.mcp: INFO
